# VabHub 测试账号说明

## 📋 测试账号信息

系统提供了以下测试账号：

### 管理员账号
- **用户名**: `admin`
- **密码**: `admin123`
- **邮箱**: `admin@vabhub.com`
- **角色**: 管理员（is_superuser = True）

### 普通用户账号1
- **用户名**: `test`
- **密码**: `test123`
- **邮箱**: `test@vabhub.com`
- **角色**: 普通用户

### 普通用户账号2
- **用户名**: `demo`
- **密码**: `demo123`
- **邮箱**: `demo@vabhub.com`
- **角色**: 普通用户

---

## 🚀 创建测试账号

### 方法1：使用批处理脚本（Windows）

双击运行：
```
创建测试账号.bat
```

### 方法2：使用Python脚本

```bash
cd backend
python create_test_user.py
```

### 方法3：使用API注册

```bash
curl -X POST "http://localhost:8000/api/v1/auth/register" \
  -H "Content-Type: application/json" \
  -d '{
    "username": "testuser",
    "email": "testuser@example.com",
    "password": "test123",
    "full_name": "测试用户"
  }'
```

---

## 🔐 登录测试

### 使用前端登录

1. 启动前端服务：
   ```bash
   cd frontend
   npm run dev
   ```

2. 访问登录页面：http://localhost:5173/login

3. 使用测试账号登录：
   - 用户名：`admin`
   - 密码：`admin123`

### 使用API登录

```bash
curl -X POST "http://localhost:8000/api/v1/auth/login" \
  -H "Content-Type: application/x-www-form-urlencoded" \
  -d "username=admin&password=admin123"
```

响应示例：
```json
{
  "access_token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
  "token_type": "bearer"
}
```

---

## 📝 使用Token访问API

获取Token后，可以在请求头中使用：

```bash
curl -X GET "http://localhost:8000/api/v1/auth/me" \
  -H "Authorization: Bearer YOUR_TOKEN_HERE"
```

---

## ⚠️ 注意事项

1. **首次使用**：需要先初始化数据库
   ```bash
   cd backend
   python manage.py
   ```

2. **账号已存在**：如果账号已存在，脚本会跳过创建

3. **生产环境**：这些测试账号仅用于开发测试，**不要在生产环境使用**

4. **密码安全**：测试账号密码简单，仅用于开发环境

---

## 🔄 重置测试账号

如果需要重置测试账号，可以：

1. 删除数据库文件（SQLite）：
   ```bash
   rm backend/vabhub.db
   ```

2. 重新初始化数据库：
   ```bash
   cd backend
   python manage.py
   ```

3. 重新创建测试账号：
   ```bash
   python create_test_user.py
   ```

---

## 📊 测试账号用途

### 管理员账号（admin）
- 测试管理员功能
- 测试权限控制
- 测试系统配置

### 普通用户账号（test/demo）
- 测试普通用户功能
- 测试用户数据隔离
- 测试多用户场景

---

## 🎯 快速开始

1. **初始化数据库**（首次使用）：
   ```bash
   cd backend
   python manage.py
   ```

2. **创建测试账号**：
   ```bash
   python create_test_user.py
   ```

3. **启动后端服务**：
   ```bash
   python -m uvicorn main:app --reload
   ```

4. **启动前端服务**：
   ```bash
   cd frontend
   npm run dev
   ```

5. **登录测试**：
   - 访问：http://localhost:5173/login
   - 用户名：`admin`
   - 密码：`admin123`

---

**完成！现在可以使用测试账号登录系统了！** 🚀

