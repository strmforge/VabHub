# 使用8001端口启动服务

**完成时间**: 2025-01-XX  
**状态**: ✅ 端口配置已更新

---

## 📋 一、端口占用情况

### ⚠️ 问题

- **8000端口**: 被占用（PID: 39960）
- **8001端口**: ✅ 空闲
- **8002端口**: ✅ 空闲

### ✅ 解决方案

使用**8001端口**启动后端服务。

---

## 📋 二、启动命令

### 启动后端服务（8001端口）

```bash
cd VabHub/backend
python -m uvicorn main:app --host 0.0.0.0 --port 8001
```

### 验证服务启动

启动后应该看到：
- ✅ "Application startup complete"
- ✅ 服务监听在8001端口
- ✅ 没有端口占用错误

---

## 📋 三、更新配置

### ✅ 已更新的文件

1. **对齐检查工具** (`tools/check_ui_backend_alignment.py`)
   - 默认端口从8000改为8001

---

## 📋 四、运行前后端对齐检查

服务启动后（等待30-60秒），运行：

```bash
# 使用8001端口
python tools/check_ui_backend_alignment.py \
  --openapi http://localhost:8001/openapi.json \
  --expected tools/ui_expected_endpoints.txt \
  --output alignment_report.json
```

或者直接运行（使用默认配置）：

```bash
python tools/check_ui_backend_alignment.py \
  --expected tools/ui_expected_endpoints.txt \
  --output alignment_report.json
```

---

## 📋 五、访问服务

### API文档

- **Swagger UI**: http://localhost:8001/docs
- **ReDoc**: http://localhost:8001/redoc
- **OpenAPI JSON**: http://localhost:8001/openapi.json

### 健康检查

```bash
curl http://localhost:8001/healthz
```

---

## 📋 六、注意事项

### 如果前端也需要更新

如果前端代码中硬编码了8000端口，需要更新为8001端口。

### 环境变量配置

可以通过环境变量配置端口：

```bash
# Windows PowerShell
$env:PORT=8001
python -m uvicorn main:app --host 0.0.0.0 --port $env:PORT

# Linux/Mac
export PORT=8001
python -m uvicorn main:app --host 0.0.0.0 --port $PORT
```

---

**文档生成时间**: 2025-01-XX  
**状态**: ✅ 端口配置已更新，可以使用8001端口启动服务

