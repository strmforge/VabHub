# 115网盘开发者平台域名配置方案

## 📋 概述

本文档说明如何在115网盘开发者平台配置域名 `vabhub115strm.cn`，以及如何将其解析到VabHub服务器，用于STRM文件重定向服务。

## 🎯 域名用途

### 1. STRM重定向服务

域名 `vabhub115strm.cn` 主要用于：

- ✅ **STRM文件302重定向**: 媒体服务器播放STRM文件时，通过此域名获取115网盘实际下载地址
- ✅ **外网访问**: 支持从外网直接访问115网盘中的视频文件
- ✅ **域名白名单**: 115网盘开发者平台可能需要验证域名所有权

### 2. 当前状态

- ✅ **域名已注册**: `vabhub115strm.cn`
- ❌ **DNS未解析**: 域名未解析到任何IP地址
- ❌ **服务器未配置**: 服务器未配置此域名
- ❌ **SSL证书未配置**: 未配置HTTPS证书（如果115要求HTTPS）

## 🔧 DNS解析配置

### 方案1：直接解析到服务器IP（推荐）

#### 1.1 获取服务器IP地址

```bash
# 如果服务器有固定公网IP
PUBLIC_IP="your-server-public-ip"

# 如果使用云服务器（阿里云、腾讯云等）
# 在云服务器控制台查看公网IP
```

#### 1.2 配置DNS解析记录

**DNS记录类型**: A记录

**记录值**:
```
类型: A
主机记录: @ 或 www
记录值: your-server-public-ip
TTL: 600 (10分钟)
```

**示例**:
```
vabhub115strm.cn        A    123.456.789.0    600
www.vabhub115strm.cn    A    123.456.789.0    600
```

#### 1.3 DNS解析服务商配置

**常见DNS服务商**:
- 阿里云DNS
- 腾讯云DNS
- Cloudflare
- 域名注册商提供的DNS服务

**配置步骤**（以阿里云为例）:
1. 登录阿里云控制台
2. 进入"云解析DNS"
3. 找到域名 `vabhub115strm.cn`
4. 添加A记录：
   - 主机记录: `@`
   - 记录类型: `A`
   - 记录值: `your-server-public-ip`
   - TTL: `600`

### 方案2：使用CDN（可选）

如果需要使用CDN加速，可以配置CNAME记录：

```
类型: CNAME
主机记录: @ 或 www
记录值: your-cdn-domain.com
TTL: 600
```

**注意**: 115网盘重定向服务通常不需要CDN，因为主要是302跳转，实际视频文件从115网盘CDN加载。

## 🌐 服务器配置

### 1. Nginx反向代理配置（推荐）

#### 1.1 安装Nginx

```bash
# Ubuntu/Debian
sudo apt update
sudo apt install nginx

# CentOS/RHEL
sudo yum install nginx
```

#### 1.2 配置Nginx

创建配置文件 `/etc/nginx/sites-available/vabhub115strm.cn`:

```nginx
server {
    listen 80;
    server_name vabhub115strm.cn www.vabhub115strm.cn;

    # 日志配置
    access_log /var/log/nginx/vabhub115strm.access.log;
    error_log /var/log/nginx/vabhub115strm.error.log;

    # 客户端最大请求体大小（用于文件上传等）
    client_max_body_size 10G;

    # STRM重定向API
    location /api/strm/ {
        proxy_pass http://127.0.0.1:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # 超时设置
        proxy_connect_timeout 300s;
        proxy_send_timeout 300s;
        proxy_read_timeout 300s;
        
        # 支持大文件传输
        proxy_buffering off;
        proxy_request_buffering off;
    }

    # 其他API（如果需要）
    location /api/ {
        proxy_pass http://127.0.0.1:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # 健康检查
    location /health {
        proxy_pass http://127.0.0.1:8000/health;
        access_log off;
    }

    # 根路径（可选）
    location / {
        return 200 "VabHub STRM Redirect Service";
        add_header Content-Type text/plain;
    }
}
```

#### 1.3 启用配置

```bash
# 创建软链接
sudo ln -s /etc/nginx/sites-available/vabhub115strm.cn /etc/nginx/sites-enabled/

# 测试配置
sudo nginx -t

# 重载Nginx
sudo systemctl reload nginx
```

### 2. SSL证书配置（HTTPS）

#### 2.1 使用Let's Encrypt免费证书

```bash
# 安装Certbot
sudo apt install certbot python3-certbot-nginx

# 申请证书
sudo certbot --nginx -d vabhub115strm.cn -d www.vabhub115strm.cn

# 自动续期
sudo certbot renew --dry-run
```

#### 2.2 更新Nginx配置支持HTTPS

```nginx
server {
    listen 80;
    server_name vabhub115strm.cn www.vabhub115strm.cn;
    
    # 重定向HTTP到HTTPS
    return 301 https://$server_name$request_uri;
}

server {
    listen 443 ssl http2;
    server_name vabhub115strm.cn www.vabhub115strm.cn;

    # SSL证书配置
    ssl_certificate /etc/letsencrypt/live/vabhub115strm.cn/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/vabhub115strm.cn/privkey.pem;
    
    # SSL优化配置
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers HIGH:!aNULL:!MD5;
    ssl_prefer_server_ciphers on;
    ssl_session_cache shared:SSL:10m;
    ssl_session_timeout 10m;

    # 其他配置同上
    # ...
}
```

### 3. 防火墙配置

```bash
# 开放80端口（HTTP）
sudo ufw allow 80/tcp

# 开放443端口（HTTPS）
sudo ufw allow 443/tcp

# 如果使用云服务器，还需要在云服务器控制台配置安全组规则
```

## 🔐 115网盘开发者平台域名验证

### 1. 域名验证要求

115网盘开发者平台可能需要验证域名所有权，常见方式：

1. **DNS TXT记录验证**
2. **文件验证**（在域名根路径放置验证文件）
3. **HTML meta标签验证**

### 2. DNS TXT记录验证

#### 2.1 添加TXT记录

```
类型: TXT
主机记录: @
记录值: 115网盘提供的验证字符串
TTL: 600
```

#### 2.2 验证记录

```bash
# 使用dig命令验证
dig TXT vabhub115strm.cn

# 或使用nslookup
nslookup -type=TXT vabhub115strm.cn
```

### 3. 文件验证

如果115网盘要求文件验证，需要在域名根路径放置验证文件：

```nginx
# 在Nginx配置中添加
location /.well-known/ {
    root /var/www/vabhub115strm;
    allow all;
}
```

```bash
# 创建验证文件目录
sudo mkdir -p /var/www/vabhub115strm/.well-known

# 创建验证文件（文件名和内容由115网盘提供）
sudo echo "verification-string" > /var/www/vabhub115strm/.well-known/115-verify.txt
```

## 📝 VabHub配置更新

### 1. 更新STRM配置

```python
# backend/app/modules/strm/config.py

class STRMConfig(BaseModel):
    """STRM系统配置"""
    # ... 现有配置 ...
    
    # 重定向服务器（使用自定义域名）
    redirect_server_url: str = 'https://vabhub115strm.cn'  # 更新为自定义域名
    redirect_server_base_path: str = '/api/strm/stream'
    
    # 或者支持环境变量配置
    # redirect_server_url: str = os.getenv('STRM_REDIRECT_URL', 'https://vabhub115strm.cn')
```

### 2. 环境变量配置

```bash
# .env文件
STRM_REDIRECT_URL=https://vabhub115strm.cn
STRM_REDIRECT_BASE_PATH=/api/strm/stream
```

### 3. 更新STRM生成器

```python
# backend/app/modules/strm/generator.py

def _generate_strm_url(self, pick_code: str, cloud_storage: str) -> str:
    """生成STRM文件URL（使用自定义域名）"""
    base_url = self.config.redirect_server_url  # https://vabhub115strm.cn
    base_path = self.config.redirect_server_base_path  # /api/strm/stream
    
    # 生成JWT token
    token = self._generate_jwt_token(pick_code, cloud_storage)
    
    return f"{base_url}{base_path}/{cloud_storage}/{token}"
```

## 🧪 测试验证

### 1. DNS解析测试

```bash
# 测试域名解析
ping vabhub115strm.cn

# 测试DNS记录
nslookup vabhub115strm.cn
dig vabhub115strm.cn
```

### 2. HTTP访问测试

```bash
# 测试HTTP访问
curl -I http://vabhub115strm.cn/health

# 测试HTTPS访问
curl -I https://vabhub115strm.cn/health
```

### 3. STRM重定向测试

```bash
# 测试STRM重定向端点
curl -I "https://vabhub115strm.cn/api/strm/stream/115/{token}"

# 应该返回302重定向
```

### 4. 115网盘域名验证

1. 登录115网盘开发者平台
2. 进入应用管理
3. 查看域名验证状态
4. 如果未验证，按照平台要求进行验证

## 📊 部署流程图

```
1. 注册域名 vabhub115strm.cn
   ↓
2. 配置DNS解析（A记录 -> 服务器IP）
   ↓
3. 在服务器上配置Nginx反向代理
   ↓
4. 申请SSL证书（Let's Encrypt）
   ↓
5. 配置HTTPS（如果需要）
   ↓
6. 在115网盘开发者平台验证域名
   ↓
7. 更新VabHub配置使用新域名
   ↓
8. 测试STRM重定向功能
   ↓
9. 生成STRM文件使用新域名
```

## 🔒 安全考虑

### 1. HTTPS强制

- ✅ **强制HTTPS**: 所有HTTP请求重定向到HTTPS
- ✅ **HSTS**: 启用HTTP严格传输安全
- ✅ **证书自动续期**: 配置Let's Encrypt自动续期

### 2. 访问控制

- ✅ **IP白名单**: 可选配置IP白名单（如果115要求）
- ✅ **限流**: 对重定向请求进行限流
- ✅ **日志记录**: 记录所有访问日志

### 3. 防火墙规则

- ✅ **仅开放必要端口**: 80（HTTP）、443（HTTPS）
- ✅ **关闭其他端口**: 避免不必要的服务暴露
- ✅ **云服务器安全组**: 配置云服务器安全组规则

## 🚀 快速部署脚本

### 1. DNS配置检查脚本

```bash
#!/bin/bash
# check_dns.sh

DOMAIN="vabhub115strm.cn"
SERVER_IP="your-server-ip"

echo "检查DNS解析..."
nslookup $DOMAIN

echo "检查A记录..."
dig +short $DOMAIN

echo "检查TXT记录（如果115要求）..."
dig TXT $DOMAIN
```

### 2. Nginx配置脚本

```bash
#!/bin/bash
# setup_nginx.sh

DOMAIN="vabhub115strm.cn"
VABHUB_PORT=8000

# 创建Nginx配置
sudo tee /etc/nginx/sites-available/$DOMAIN > /dev/null <<EOF
server {
    listen 80;
    server_name $DOMAIN www.$DOMAIN;
    
    location /api/strm/ {
        proxy_pass http://127.0.0.1:$VABHUB_PORT;
        proxy_set_header Host \$host;
        proxy_set_header X-Real-IP \$remote_addr;
        proxy_set_header X-Forwarded-For \$proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto \$scheme;
    }
}
EOF

# 启用配置
sudo ln -s /etc/nginx/sites-available/$DOMAIN /etc/nginx/sites-enabled/

# 测试配置
sudo nginx -t

# 重载Nginx
sudo systemctl reload nginx
```

### 3. SSL证书申请脚本

```bash
#!/bin/bash
# setup_ssl.sh

DOMAIN="vabhub115strm.cn"

# 申请证书
sudo certbot --nginx -d $DOMAIN -d www.$DOMAIN --non-interactive --agree-tos --email your-email@example.com

# 配置自动续期
sudo systemctl enable certbot.timer
sudo systemctl start certbot.timer
```

## 📚 相关文档

- [VabHub STRM文件链接格式设计](./VabHub%20STRM文件链接格式设计.md)
- [115网盘官方API文档集成完成总结](./115网盘官方API文档集成完成总结.md)
- [115网盘OAuth2令牌刷新机制说明](./115网盘OAuth2令牌刷新机制说明.md)

## ✅ 检查清单

### DNS配置
- [ ] 域名已注册
- [ ] DNS A记录已配置
- [ ] DNS解析生效（可通过nslookup验证）
- [ ] TXT记录已配置（如果115要求）

### 服务器配置
- [ ] Nginx已安装
- [ ] Nginx配置文件已创建
- [ ] 反向代理已配置
- [ ] 防火墙规则已配置
- [ ] 端口已开放（80、443）

### SSL证书
- [ ] SSL证书已申请
- [ ] HTTPS已配置
- [ ] HTTP自动重定向到HTTPS
- [ ] 证书自动续期已配置

### 115网盘平台
- [ ] 域名已在115网盘开发者平台提交
- [ ] 域名验证已完成
- [ ] 应用配置已更新

### VabHub配置
- [ ] STRM配置已更新（使用新域名）
- [ ] 环境变量已配置
- [ ] STRM生成器已更新
- [ ] 测试验证已完成

## 🎯 下一步

1. ✅ **配置DNS解析**: 将域名解析到服务器IP
2. ✅ **配置Nginx**: 设置反向代理
3. ✅ **申请SSL证书**: 配置HTTPS（如果115要求）
4. ✅ **验证域名**: 在115网盘开发者平台验证域名
5. ✅ **更新配置**: 更新VabHub配置使用新域名
6. ✅ **测试验证**: 测试STRM重定向功能

## ⚠️ 注意事项

1. **域名解析时间**: DNS解析可能需要几分钟到几小时生效
2. **SSL证书申请**: Let's Encrypt证书申请需要域名可访问
3. **115平台验证**: 115网盘开发者平台可能需要时间审核域名
4. **HTTPS要求**: 如果115要求HTTPS，必须配置SSL证书
5. **IP变更**: 如果服务器IP变更，需要更新DNS A记录

## 🔗 相关链接

- [115网盘开发者平台](https://open.115.com/)
- [Let's Encrypt](https://letsencrypt.org/)
- [Nginx文档](https://nginx.org/en/docs/)

