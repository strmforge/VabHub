# 正确执行顺序

## ⚠️ 重要提示

**命令必须按顺序执行！** 先切换目录，再执行npm命令。

---

## ❌ 错误的执行顺序

```powershell
npm run dev              # ❌ 错误：在错误的目录中执行
npm install --legacy-peer-deps  # ❌ 错误：在错误的目录中执行
cd "F:\VabHub项目\VabHub\frontend"  # ❌ 太晚了，应该在前面
```

---

## ✅ 正确的执行顺序

### 步骤1: 先切换目录

```powershell
cd "F:\VabHub项目\VabHub\frontend"
```

**按回车，等待执行完成。**

### 步骤2: 验证目录

```powershell
pwd
```

应该显示：`F:\VabHub项目\VabHub\frontend`

或者：

```powershell
ls package.json
```

如果能看到 `package.json`，说明在正确的目录中。

### 步骤3: 安装依赖

```powershell
npm install --legacy-peer-deps
```

**按回车，等待安装完成**（可能需要几分钟）。

### 步骤4: 启动服务

```powershell
npm run dev
```

**按回车，等待启动完成。**

---

## 📋 完整命令序列（按顺序执行）

**重要**: 每次只执行一条命令，等待完成后再执行下一条！

```powershell
# 第1条：切换目录（必须先执行）
cd "F:\VabHub项目\VabHub\frontend"

# 第2条：验证目录（可选，但推荐）
pwd

# 第3条：安装依赖（等待完成）
npm install --legacy-peer-deps

# 第4条：启动服务（等待完成）
npm run dev
```

---

## 🎯 推荐操作

### 方法1: 在文件资源管理器中打开（最简单）

1. **打开文件资源管理器**（Win+E）
2. **导航到**: `F:\VabHub项目\VabHub\frontend`
3. **在地址栏输入**: `powershell`，按回车
4. **现在你已经在正确的目录中了！**
5. **执行**: `npm install --legacy-peer-deps`
6. **等待安装完成**
7. **执行**: `npm run dev`

### 方法2: 使用PowerShell（按正确顺序）

1. **打开PowerShell**（Win+X）
2. **先执行**: `cd "F:\VabHub项目\VabHub\frontend"`
3. **验证**: `pwd` 或 `ls package.json`
4. **执行**: `npm install --legacy-peer-deps`
5. **等待安装完成**
6. **执行**: `npm run dev`

---

## ✅ 验证是否在正确目录

执行以下命令检查：

```powershell
# 查看当前目录
pwd

# 或者查看package.json是否存在
ls package.json
```

如果 `pwd` 显示 `F:\VabHub项目\VabHub\frontend`，或者能看到 `package.json`，说明在正确的目录中。

---

## 🐛 如果还是出错

### 检查1: 确认目录切换成功

```powershell
pwd
```

如果显示的不是 `F:\VabHub项目\VabHub\frontend`，说明切换失败。

### 检查2: 确认package.json存在

```powershell
ls package.json
```

如果显示"找不到文件"，说明不在正确的目录中。

### 检查3: 使用绝对路径

如果路径切换有问题，可以尝试：

```powershell
Set-Location "F:\VabHub项目\VabHub\frontend"
```

---

## 📝 成功标志

如果看到以下内容，说明启动成功：

```
VITE v5.x.x  ready in xxx ms

➜  Local:   http://localhost:5173/
➜  Network: http://192.168.x.x:5173/
```

然后：
1. 打开浏览器
2. 访问 http://localhost:5173
3. 应该能看到VabHub登录页面

---

## 🎯 立即执行

**按照以下顺序执行**：

1. **打开PowerShell**（Win+X）
2. **执行**: `cd "F:\VabHub项目\VabHub\frontend"`
3. **验证**: `pwd`（应该显示frontend目录）
4. **执行**: `npm install --legacy-peer-deps`
5. **等待安装完成**
6. **执行**: `npm run dev`

---

**记住：先切换目录，再执行npm命令！** 🚀

