# 115网盘文件操作API官方文档对照

## 📋 官方文档规范

### 1. 文件复制API

**端点**: `POST 域名+/open/ufile/copy`

**说明**: 批量复制文件

#### 请求参数

**Headers**:
- `Authorization: Bearer access_token`（必填）

**Body (form-data)**:
- `pid`: 目标目录ID（text类型，必填）
- `file_id`: 文件(夹)ID（text类型，必填，多个文件和目录请以,隔开）
- `nodupli`: 是否允许重复（text类型，可选，默认0:可以;1:不可以）

#### 返回数据

- `state`: boolean（接口状态）
- `message`: string（异常信息）
- `code`: number（异常码）
- `data`: array（数据）

### 2. 文件移动API

**端点**: `POST 域名+/open/ufile/move`

**说明**: 批量移动文件

#### 请求参数

**Headers**:
- `Authorization: Bearer access_token`（必填）

**Body (form-data)**:
- `file_ids`: 文件(夹)ID（string类型，必填，多个以,隔开）
- `to_cid`: 目标目录ID（string类型，必填，根目录为0）

#### 返回数据

- `state`: boolean（接口状态）
- `message`: string（异常信息）
- `code`: number（异常码）
- `data`: array（数据）

### 3. 文件(夹)更新API

**端点**: `POST 域名+/open/ufile/update`

**说明**: 更新文件名或星标文件

#### 请求参数

**Headers**:
- `Authorization: Bearer access_token`（必填）

**Body (form-data)**:
- `file_id`: 文件(夹)ID（text类型，必填）
- `file_name`: 新的文件(夹)名字（text类型，可选，文件夹名称限制255字节）
- `star`: 是否星标（text类型，可选，1:星标;0:取消星标）

#### 返回数据

- `state`: boolean（接口状态）
- `message`: string（异常信息）
- `code`: number（异常码）
- `data`: object
  - `file_name`: string（新的文件(夹)名字）
  - `star`: string（文件星标状态）

### 4. 删除文件API

**端点**: `POST 域名+/open/ufile/delete`

**说明**: 批量删除文件(夹)

#### 请求参数

**Headers**:
- `Authorization: Bearer access_token`（必填）

**Body (form-data)**:
- `file_ids`: 文件(夹)ID（text类型，必填，多个以,隔开）
- `parent_id`: 父目录ID（text类型，可选）

#### 返回数据

- `state`: boolean（接口状态）
- `message`: string（异常信息）
- `code`: string（异常码）
- `data`: string[]（数据，字符串数组）

### 5. 获取文件下载地址API

**端点**: `POST 域名+/open/ufile/downurl`

**说明**: 根据文件提取码获取文件下载地址

#### 请求参数

**Headers**:
- `Authorization: Bearer access_token`（必填）

**Body (form-data)**:
- `pick_code`: 文件提取码（text类型，必填）

#### 返回数据

- `state`: boolean（接口状态）
- `message`: string（异常信息）
- `code`: number（异常码）
- `data`: object
  - `{file_id}`: object（文件ID作为key）
    - `file_name`: string（文件名）
    - `file_size`: number（文件大小）
    - `pick_code`: string（文件提取码）
    - `sha1`: string（文件sha1值）
    - `url`: object
      - `url`: string（文件下载地址）

## ✅ 当前实现对照

### 1. 文件复制API ✅

#### 实现检查

- **API端点**: ✅ 正确 (`POST /open/ufile/copy`)
- **请求头**: ✅ 正确 (`Authorization: Bearer access_token`)
- **请求参数**: ✅ 正确
  - `pid`: ✅ 支持（target_parent_id参数）
  - `file_id`: ✅ 支持（file_id或file_ids参数，支持单个和批量）
  - `nodupli`: ✅ 支持（allow_duplicate参数）
- **返回数据**: ✅ 正确（返回success和data）

#### 改进点

1. ✅ **批量复制**: 支持单个和批量复制
2. ✅ **重复控制**: 支持allow_duplicate参数控制是否允许重复
3. ✅ **参数类型**: 使用text类型（字符串）

### 2. 文件移动API ✅

#### 实现检查

- **API端点**: ✅ 正确 (`POST /open/ufile/move`)
- **请求头**: ✅ 正确 (`Authorization: Bearer access_token`)
- **请求参数**: ✅ 正确
  - `file_ids`: ✅ 支持（file_id或file_ids参数，支持单个和批量）
  - `to_cid`: ✅ 支持（target_parent_id参数）
- **返回数据**: ✅ 正确（返回success和data）

#### 改进点

1. ✅ **批量移动**: 支持单个和批量移动
2. ✅ **参数类型**: 使用string类型
3. ✅ **根目录支持**: 支持根目录（to_cid=0）

### 3. 文件(夹)更新API ✅

#### 实现检查

- **API端点**: ✅ 正确 (`POST /open/ufile/update`)
- **请求头**: ✅ 正确 (`Authorization: Bearer access_token`)
- **请求参数**: ✅ 正确
  - `file_id`: ✅ 支持
  - `file_name`: ✅ 支持（可选）
  - `star`: ✅ 支持（可选）
- **返回数据**: ✅ 正确（返回file_name和star）
- **便捷方法**: ✅ 添加了`rename_file`和`star_file`便捷方法

#### 改进点

1. ✅ **文件名更新**: 支持更新文件名
2. ✅ **星标管理**: 支持设置和取消星标
3. ✅ **名称验证**: 验证文件夹名称长度（限制255字节）
4. ✅ **便捷方法**: 提供`rename_file`和`star_file`便捷方法

### 4. 删除文件API ✅

#### 实现检查

- **API端点**: ✅ 正确 (`POST /open/ufile/delete`)
- **请求头**: ✅ 正确 (`Authorization: Bearer access_token`)
- **请求参数**: ✅ 正确
  - `file_ids`: ✅ 支持（file_id或file_ids参数，支持单个和批量）
  - `parent_id`: ✅ 支持（可选）
- **返回数据**: ✅ 正确（返回data数组，字符串数组）

#### 改进点

1. ✅ **批量删除**: 支持单个和批量删除
2. ✅ **父目录支持**: 支持parent_id参数
3. ✅ **返回格式**: 正确处理data为字符串数组

### 5. 获取文件下载地址API ✅

#### 实现检查

- **API端点**: ✅ 正确 (`POST /open/ufile/downurl`)
- **请求头**: ✅ 正确 (`Authorization: Bearer access_token`)
- **请求参数**: ✅ 正确
  - `pick_code`: ✅ 支持
- **返回数据**: ✅ 正确（解析file_id作为key的对象结构）

#### 改进点

1. ✅ **下载地址**: 支持根据pick_code获取下载地址
2. ✅ **数据解析**: 正确解析file_id作为key的对象结构
3. ✅ **URL提取**: 正确提取url对象中的url字段

## 💡 使用示例

### 1. 复制文件

```python
from app.core.cloud_storage.providers.cloud_115_api import Cloud115API

# 初始化API客户端
api = Cloud115API(access_token=token_info["access_token"])

# 单个文件复制
result = await api.copy_file(
    file_id="2323423573680609857",
    target_parent_id="1054251402869818368",
    allow_duplicate=True
)

# 批量文件复制
result = await api.copy_file(
    file_ids=["2323423573680609857", "2323423573680609858"],
    target_parent_id="1054251402869818368",
    allow_duplicate=False  # 不允许重复
)
```

### 2. 移动文件

```python
# 单个文件移动
result = await api.move_file(
    file_id="3073323042143855813",
    target_parent_id="3073311192547189943"
)

# 批量文件移动
result = await api.move_file(
    file_ids=["3073323042143855813", "3073323042143855822"],
    target_parent_id="0"  # 根目录
)
```

### 3. 更新文件

```python
# 重命名文件
result = await api.update_file(
    file_id="3073323042143855813",
    file_name="新的名字"
)

# 设置星标
result = await api.update_file(
    file_id="3073323042143855813",
    star=1  # 1:星标, 0:取消星标
)

# 同时更新文件名和星标
result = await api.update_file(
    file_id="3073323042143855813",
    file_name="新的名字",
    star=1
)

# 使用便捷方法
await api.rename_file(file_id="3073323042143855813", new_name="新的名字")
await api.star_file(file_id="3073323042143855813", star=True)
```

### 4. 删除文件

```python
# 单个文件删除
result = await api.delete_file(
    file_ids="3073323042143855813",
    parent_id="3073311192547189943"  # 可选
)

# 批量文件删除
result = await api.delete_file(
    file_ids=["3073323042143855813", "3073323042143855822"]
)
```

### 5. 获取文件下载地址

```python
# 根据pick_code获取下载地址
result = await api.get_download_url(pick_code="dtctprlmfkl4exiok")

# 返回结果
# {
#     "success": True,
#     "file_id": "2323423573680609857",
#     "file_name": "file.mkv",
#     "file_size": 1610612736,
#     "pick_code": "dtctprlmfkl4exiok",
#     "sha1": "abc123...",
#     "download_url": "https://..."
# }

# 使用下载地址
if result.get("success"):
    download_url = result.get("download_url")
    # 使用download_url下载文件
```

## 📊 参数对照表

### 文件复制API

| 官方文档参数 | 当前实现参数 | 类型 | 说明 |
|-------------|-------------|------|------|
| pid | target_parent_id | string | 目标目录ID |
| file_id | file_id/file_ids | string/List[string] | 文件ID（支持单个和批量） |
| nodupli | allow_duplicate | bool | 是否允许重复（0:可以,1:不可以） |

### 文件移动API

| 官方文档参数 | 当前实现参数 | 类型 | 说明 |
|-------------|-------------|------|------|
| file_ids | file_id/file_ids | string/List[string] | 文件ID（支持单个和批量） |
| to_cid | target_parent_id | string | 目标目录ID（根目录为0） |

### 文件(夹)更新API

| 官方文档参数 | 当前实现参数 | 类型 | 说明 |
|-------------|-------------|------|------|
| file_id | file_id | string | 文件ID |
| file_name | file_name | string | 新名称（可选） |
| star | star | int | 星标状态（可选，1:星标,0:取消星标） |

### 删除文件API

| 官方文档参数 | 当前实现参数 | 类型 | 说明 |
|-------------|-------------|------|------|
| file_ids | file_id/file_ids | string/List[string] | 文件ID（支持单个和批量） |
| parent_id | parent_id | string | 父目录ID（可选） |

### 获取文件下载地址API

| 官方文档参数 | 当前实现参数 | 类型 | 说明 |
|-------------|-------------|------|------|
| pick_code | pick_code | string | 文件提取码 |

## 🎯 关键改进

### 1. 批量操作支持

1. ✅ **批量复制**: 支持单个和批量复制
2. ✅ **批量移动**: 支持单个和批量移动
3. ✅ **批量删除**: 支持单个和批量删除

### 2. 参数验证

1. ✅ **文件名长度**: 验证文件夹名称长度（限制255字节）
2. ✅ **参数类型**: 使用正确的参数类型（text/string）
3. ✅ **必填参数**: 验证必填参数

### 3. 返回数据

1. ✅ **统一格式**: 所有方法返回统一的格式（success, data, error）
2. ✅ **数据解析**: 正确解析返回数据
3. ✅ **错误处理**: 完善的错误处理

### 4. 便捷方法

1. ✅ **rename_file**: 重命名文件的便捷方法
2. ✅ **star_file**: 设置星标的便捷方法

## ✅ 结论

当前实现**完全符合**官方文档规范：

1. ✅ **文件复制API**: 完全符合，支持批量复制和重复控制
2. ✅ **文件移动API**: 完全符合，支持批量移动
3. ✅ **文件(夹)更新API**: 完全符合，支持文件名和星标更新
4. ✅ **删除文件API**: 完全符合，支持批量删除
5. ✅ **获取文件下载地址API**: 完全符合，支持根据pick_code获取下载地址

**总体评价**: ✅ **实现完整，完全符合官方文档规范**

## 🔗 相关文档

- [115网盘官方API文档](https://www.yuque.com/115yun/open/qur839kyx9cgxpxi)
- [文件操作API官方文档对照](./115网盘文件操作API官方文档对照.md)
- [文件列表API官方文档对照](./115网盘文件列表和文件详情API官方文档对照.md)
- [文件搜索API官方文档对照](./115网盘文件搜索API官方文档对照.md)
- [API客户端使用](./115网盘官方API文档集成完成总结.md)
