# DOCKER-DEPLOY-SIMPLE-1 自测步骤

## 环境准备

使用全新环境（或删除旧 volume）进行测试。

## 自测流程

### 步骤 1：准备配置

```bash
cp .env.docker.example .env.docker
```

编辑 `.env.docker`，仅修改：
- `DB_PASSWORD`：设置一个强密码

### 步骤 2：修改挂载路径

编辑 `docker-compose.yml`，将 `/volume1/...` 改为你的实际路径。

### 步骤 3：启动服务

```bash
docker compose up -d
```

### 步骤 4：验证点

#### 4.1 密钥自动生成
```bash
docker logs vabhub 2>&1 | grep -i "密钥"
```
预期输出：看到"密钥管理器已初始化"相关信息

#### 4.2 初始管理员创建
```bash
docker logs vabhub 2>&1 | grep -i "初始管理员"
```
预期输出：
```
🔐 初始管理员账号已创建
   用户名: admin
   密码: <随机密码>
```

#### 4.3 Web UI 可访问
```bash
curl http://localhost:52180/health
```
预期输出：`{"status": "healthy", ...}`

浏览器访问 http://localhost:52180，应能打开登录页

#### 4.4 管理员登录
使用日志中的 admin 账号和随机密码登录，应能成功进入系统

### 步骤 5：验证 ENV 设置密码

重新测试，这次在 `.env.docker` 中设置：

```bash
SUPERUSER_PASSWORD=test123456
```

然后重建容器：

```bash
docker compose down
docker volume rm vabhub_data  # 清除数据以触发重新创建
docker compose up -d
```

验证：
- 日志中不应输出随机密码
- 使用 `admin` / `test123456` 应能登录

## 验证挂载路径

确认以下目录在宿主机上产生：
- `/your/path/vabhub/app-data`
- `/your/path/vabhub/logs`
- `/your/path/vabhub/postgres`
- `/your/path/vabhub/redis`

## 测试结果记录

| 验证项 | 状态 | 备注 |
|--------|------|------|
| 密钥自动生成 | ⏳ | |
| 初始管理员创建 | ⏳ | |
| Web UI 可访问 | ⏳ | |
| 管理员登录成功 | ⏳ | |
| ENV 密码生效 | ⏳ | |
| 挂载路径正确 | ⏳ | |

---

*Created: 2025-12-06*
