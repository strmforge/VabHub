# VabHub Docker 部署环境变量示例
# 复制此文件为 .env.docker 并根据需要修改配置
# 文档参考: docs/DEPLOY_WITH_DOCKER.md

# ============== Core & Database ==============
# [required] 应用密钥（首次启动会自动生成安全密钥）
SECRET_KEY=change-this-in-production-use-strong-random-string
JWT_SECRET_KEY=change-this-in-production-use-another-strong-random-string

# [required] 数据库配置（Docker 部署）
DB_HOST=db
DB_PORT=5432
DB_NAME=vabhub
DB_USER=vabhub
# ⚠️ 安全警告：请务必修改此密码！
DB_PASSWORD=CHANGE-ME-use-strong-random-password
DATABASE_URL=postgresql://vabhub:CHANGE-ME-use-strong-random-password@db:5432/vabhub

# [recommended] 应用配置
# 默认端口 52180（避开常见下载器端口）
VABHUB_PORT=52180
APP_BASE_URL=http://localhost:52180
APP_WEB_BASE_URL=http://localhost:52180
PORT=52180
WORKERS=4
APP_DEMO_MODE=false

# ============== Redis 配置 ==============
# [recommended] Redis 缓存加速
REDIS_URL=redis://redis:6379/0
REDIS_ENABLED=true

# ============== 存储路径配置 ==============
# 基础存储路径（在 Docker 容器内）
STORAGE_PATH=/app/data
TEMP_PATH=/app/tmp
EBOOK_LIBRARY_ROOT=/app/data/ebooks

# ============== 外部 API 配置 ==============
# [required] TMDB API Key（影视元数据）
# 获取地址: https://www.themoviedb.org/settings/api
TMDB_API_KEY=your-tmdb-api-key-here

# ============== 前端配置 ==============
# API 基础 URL（前端使用，指向后端服务）
# All-in-One 模式使用相对路径 /api
VITE_API_BASE_URL=/api

# ============== Docker 相关 ==============
# Compose 项目名称
COMPOSE_PROJECT_NAME=vabhub

# 镜像版本（用于 docker-compose.prod.yml 拉取官方镜像）
# 升级时只需修改此版本号，然后 docker compose pull && docker compose up -d
VABHUB_VERSION=0.0.1-rc1

# ============== AI Orchestrator ====================
# AI 总控层配置（可选，需外部 LLM）
AI_ORCH_ENABLED=false
AI_ORCH_LLM_PROVIDER=http
AI_ORCH_LLM_ENDPOINT=
AI_ORCH_LLM_API_KEY=
AI_ORCH_LLM_MODEL=

# ============== Telegram Bot 配置（可选） ==============
TELEGRAM_BOT_TOKEN=
TELEGRAM_BOT_ENABLED=false
TELEGRAM_BOT_PROXY=
TELEGRAM_BOT_ALLOWED_USERS=

# ============== 下载器配置（可选） ==============
# qBittorrent 配置
# QB_URL=http://qbittorrent:8080
# QB_USERNAME=admin
# QB_PASSWORD=adminadmin

# Transmission 配置
# TR_URL=http://transmission:9091
# TR_USERNAME=admin
# TR_PASSWORD=admin

# ============== RSSHub 配置 ==============
RSSHUB_ENABLED=true
RSSHUB_BASE_URL=https://rsshub.app

# ============== 通知推送配置 ==============
NOTIFY_TELEGRAM_DOWNLOAD_SUBSCRIPTION=false
NOTIFY_TELEGRAM_DOWNLOAD_COMPLETION=false
NOTIFY_TELEGRAM_DOWNLOAD_HR_RISK=true
