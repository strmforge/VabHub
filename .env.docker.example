# ============================================================================
# VabHub Docker 部署环境变量示例
# ============================================================================
# 复制此文件为 .env.docker 并根据需要修改配置
# 文档参考: docs/user/DEPLOY_WITH_DOCKER.md
#
# 配置分类说明:
#   [必填] - 必须修改，否则无法正常运行
#   [推荐] - 建议设置，提升体验
#   [可选] - 按需配置
#   [高级] - 一般用户无需修改，系统会自动处理
# ============================================================================

# ============== [必填] 数据库配置 ==============
# ⚠️ 安全警告：请务必修改此密码！
DB_PASSWORD=CHANGE-ME-use-strong-random-password

# 数据库连接参数（一般无需修改）
DB_HOST=db
DB_PORT=5432
DB_NAME=vabhub
DB_USER=vabhub

# ============== [推荐] 初始管理员配置 ==============
# 首次启动时自动创建管理员账号
# 如果不设置密码，系统会生成随机密码并输出到容器日志
# 查看方式: docker logs vabhub | grep "初始管理员"
# SUPERUSER_NAME=admin
# SUPERUSER_PASSWORD=请修改为你自己的初始密码

# ============== [推荐] TMDB API Key ==============
# 影视元数据获取（电影/剧集海报、简介等）
# 获取地址: https://www.themoviedb.org/settings/api
# 不配置也可运行，但会影响影视识别和海报显示
TMDB_API_KEY=

# ============== [可选] 应用配置 ==============
# 默认端口 52180（避开常见下载器端口 8080/7878/8989/9091）
VABHUB_PORT=52180
APP_BASE_URL=http://localhost:52180
TZ=Asia/Shanghai

# ============== [可选] Redis 配置 ==============
REDIS_URL=redis://redis:6379/0
REDIS_ENABLED=true

# ============== [可选] Telegram Bot ==============
# 从 @BotFather 获取 Token
TELEGRAM_BOT_TOKEN=
TELEGRAM_BOT_ENABLED=false
# TELEGRAM_BOT_PROXY=http://127.0.0.1:7890

# ============== [可选] AI 总控层 ==============
# 需要外部 LLM API（OpenAI 兼容）
AI_ORCH_ENABLED=false
# AI_ORCH_LLM_ENDPOINT=https://api.openai.com/v1/chat/completions
# AI_ORCH_LLM_API_KEY=your-api-key
# AI_ORCH_LLM_MODEL=gpt-4o-mini

# ============== [可选] 下载器配置 ==============
# qBittorrent
# QB_URL=http://qbittorrent:8080
# QB_USERNAME=admin
# QB_PASSWORD=adminadmin

# Transmission
# TR_URL=http://transmission:9091
# TR_USERNAME=admin
# TR_PASSWORD=admin

# ============== [可选] RSSHub ==============
RSSHUB_ENABLED=true
RSSHUB_BASE_URL=https://rsshub.app

# ============== [可选] 通知推送 ==============
NOTIFY_TELEGRAM_DOWNLOAD_SUBSCRIPTION=false
NOTIFY_TELEGRAM_DOWNLOAD_COMPLETION=false
NOTIFY_TELEGRAM_DOWNLOAD_HR_RISK=true

# ============== [高级] 安全密钥 ==============
# 默认自动生成并持久化到 /app/data/.vabhub_secrets.json
# 一般用户无需配置，重启后会复用已生成的密钥
# 仅在需要自定义密钥时取消注释并设置
# SECRET_KEY=your-custom-secret-key
# JWT_SECRET_KEY=your-custom-jwt-secret-key

# ============== [高级] Docker 相关 ==============
COMPOSE_PROJECT_NAME=vabhub
VABHUB_VERSION=0.0.1-rc1
